"""Telemetry — the ONLY synthetic part of KALMAN.

A realistic stream-health simulator with on-command fault injection. It
feeds Grafana Cloud (via Alloy remote_write); everything downstream is real.
"""
